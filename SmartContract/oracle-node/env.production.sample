# Production configuration for VPS
# Copy this to .env on VPS: cp env.production.sample .env

# Pione Zero Testnet configuration
RPC_URL=https://rpc.zeroscan.org
CHAIN_ID=5080

# Wallet configuration - CHANGE THESE
PRIVATE_KEY=0xYOUR_PRIVATE_KEY   # ví testnet từ MetaMask
CONTRACT_ADDRESS=0xCONTRACT_AFTER_DEPLOY  # Địa chỉ contract đã deploy

# Server configuration
PORT=5001

# Note: In production, consider using environment variables
# or secrets management instead of storing private key in file






